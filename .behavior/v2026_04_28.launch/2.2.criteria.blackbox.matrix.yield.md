# blackbox criteria coverage matrix

## matrix.1 = getConfig method behavior (usecase 1, 2)

| ind: method | dep: fills $.at() | dep: validates schema | dep: async |
|-------------|-------------------|----------------------|------------|
| getConfig() | yes | yes | yes |
| getConfig.filled() | yes | yes | yes |
| getConfig.static() | no | no | no |

**gaps:** none — all methods have defined behavior

---

## matrix.2 = validation by environment (usecase 3)

| ind: environment | ind: schema matches | dep: behavior |
|------------------|---------------------|---------------|
| test/* | yes | return config |
| test/* | no | failfast |
| prep/* | yes | return config |
| prep/* | no | failfast |
| prod/local | yes | return config |
| prod/local | no | failfast |
| prod/cloud | yes | return config |
| prod/cloud | no | warn, return config |

**gaps:** none — all environment × schema combinations covered

---

## matrix.3 = $.at() uri expansion (usecase 4, 5, 6, 10)

| ind: uri format | ind: supplier registered | dep: behavior |
|-----------------|--------------------------|---------------|
| $.at(aws::param) | yes | auto-derive /repo/env/key.path |
| $.at(aws::param) | no | failfast (no supplier) |
| $.at(aws::param/explicit/path) | yes | expand to /explicit/path |
| $.at(aws::param/explicit/path) | no | failfast (no supplier) |
| $.at(aws::secret) | yes | auto-derive via secrets manager |
| $.at(aws::secret) | no | failfast (no supplier) |
| $.at(aws::secret/explicit/path) | yes | expand to explicit path |
| $.at(aws::secret/explicit/path) | no | failfast (no supplier) |
| $.at(unknown::x) | n/a | failfast (unknown scheme) |

**gaps:** none — all uri × supplier combinations covered

---

## matrix.4 = cache behavior (usecase 8)

| ind: time since last call | dep: source |
|---------------------------|-------------|
| < TTL | cache |
| >= TTL | fresh fetch |

**gaps:** none — both cache hit and miss covered

---

## matrix.5 = repo name derivation (usecase 9)

| ind: explicit repoName in input | dep: source |
|---------------------------------|-------------|
| yes | explicit repoName |
| no | package.json name |

**gaps:** none — both explicit and implicit covered

---

## matrix.6 = config file selection (usecase 11)

| ind: environment.access | dep: file loaded |
|-------------------------|------------------|
| test | config/test.yml |
| prep | config/prep.yml |
| prod | config/prod.yml |

**note:** prod/local and prod/cloud both have access="prod", same file

**gaps:** none — all access values covered

---

## matrix.7 = supplier failure behavior (usecase 7)

| ind: ssm call result | ind: retry result | dep: behavior |
|----------------------|-------------------|---------------|
| success | n/a | return value |
| fail | success | return value |
| fail | fail | failfast |

**gaps:** none — success, retry-success, and retry-fail all covered

---

## decomposition analysis

**dimension counts:**
- matrix.1: 1 dimension (method) — clean
- matrix.2: 2 dimensions (environment, schema) — clean
- matrix.3: 2 dimensions (uri, supplier) — clean
- matrix.4: 1 dimension (time) — clean
- matrix.5: 1 dimension (explicit?) — clean
- matrix.6: 1 dimension (access) — clean
- matrix.7: 2 dimensions (call, retry) — clean

**no decomposition needed** — all matrices have ≤2 independent dimensions
