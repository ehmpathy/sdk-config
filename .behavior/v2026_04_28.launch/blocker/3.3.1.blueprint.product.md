# blocker: 3.3.1.blueprint.product

## what blocks

the route requires formal human approval before passage. the `approved?` judge gate requires:

```
rhx route.stone.set --stone 3.3.1.blueprint.product --as approved
```

this command can only be run by the human, not by the mechanic.

## what i tried

1. completed blueprint at `.behavior/v2026_04_28.launch/3.3.1.blueprint.product.yield.md`
2. completed all 15 self-review guards
3. signaled `--as arrived` which triggered peer review
4. peer review passed:
   - ✓ review.1 (failhides check)
   - ✓ review.2-5 (architectural + experiential reviews)
   - ✓ judge.1 (reviewed? — 0 blockers, ≤3 nitpicks)
   - ✗ judge.2 (approved? — needs human)
5. human verbally said "approved!" but the formal command was not run
6. tried `--as passed` which showed "wait for human approval"

## what i need

human to run the formal approval command:

```
rhx route.stone.set --stone 3.3.1.blueprint.product --as approved
```

after human runs this, i can signal `--as passed` to proceed to the next stone.
