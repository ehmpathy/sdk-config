# 5.3 verification yield

> buttonup gate — prove the deliverable via tests, fix every gap found. this
> stone closed three concrete test-coverage gaps surfaced by the i19 l3 peer
> reviews (r10/r11) and re-proved every suite green, incl. acceptance vs REAL AWS.

## verification checklist

### behavior coverage (wish + vision → tests)

| behavior (from wish/vision) | test file | snapshots? | status |
|-----------------------------|-----------|------------|--------|
| optional field denied → tolerated, key omitted | genGetConfig.test.ts case13; acceptance case7/case13 | ✓ | ✓ |
| optional field absent (not-found) → tolerated | genGetConfig.test.ts case20; acceptance case11 | ✓ | ✓ |
| required field denied → hard-throw, denied path in context | genGetConfig.test.ts case14; acceptance case8 | ✓ | ✓ |
| required-denied hard-throws even in prod/cloud | genGetConfig.test.ts case15 | ✓ | ✓ |
| nullable-only denied → hard-throw (no null-fill) | genGetConfig.test.ts case17; acceptance case10 | ✓ (added) | ✓ |
| ancestor-optional, denied required leaf → hard-throw | genGetConfig.test.ts case16; acceptance case9 | ✓ (added) | ✓ |
| optional + default, denied → default applies (Q1) | genGetConfig.test.ts case18; acceptance case12 | ✓ | ✓ |
| nullish denied → omitted (undefined tolerated) | genGetConfig.test.ts case19; acceptance case11 | ✓ | ✓ |
| transient (throttle/network) on ANY field → never masked | asFilledConfig.test.ts case8; genGetConfig.test.ts case24 (added) | n/a (throws) | ✓ |
| two required paths denied → aggregate into one throw | genGetConfig.test.ts case23 | ✓ | ✓ |
| denied-required + unrelated drift → not mis-attributed | genGetConfig.test.ts case25 (added) | ✓ (added) | ✓ |
| present-but-empty is NOT tolerable, even if optional | genGetConfig.test.ts case22 | n/a (throws) | ✓ |
| cicd grant escalation (plan/apply), both jobs + boundaries | genGetConfig.test.ts case21 t0–t3; acceptance case14 t0–t3 REAL AWS | ✓ | ✓ |
| custom supplier opts into tolerance via public SupplyDeniedError | acceptance case13 | ✓ | ✓ |
| no denial → byte-identical to today (no regression) | acceptance case1/3/5/6 | ✓ | ✓ |
| real AWS AccessDeniedException → SupplyDeniedError | genSdkConfigSupplierAwsParameterStore.test.ts case5; …SecretsManager.test.ts | ✓ | ✓ (unit, real error shape) |
| real AWS ParameterNotFound / ResourceNotFound → SupplyAbsentError | supplier unit case4 + acceptance absent branch REAL AWS | ✓ | ✓ |

### gaps closed this stone (from i19 l3 reviews)

| gap (reviewer) | fix |
|----------------|-----|
| r10 #3.4 — case16/case17 assert `.toContain` but lack `toMatchSnapshot()` (repo convention: both) | added `toMatchSnapshot()` to both; snapshots generated |
| r10 #3.2 — "transient never masked" had no end-to-end getConfig() proof for an OPTIONAL field | added case24: transient error on optional field propagates through full public getConfig() |
| r10 #3.3 — mixed failure (denied-required + unrelated drift) untested | added case25: proves throw is 'could not be read', names the denied leaf, surfaces the drift in errors, and denials holds ONLY the denial (snapshot proves non-mis-attribution) |

### zero skips verified
- [x] no `.skip()` or `.only()` found (grep `\.(skip|only)\(` in src → no matches)
- [x] no silent credential bypasses (acceptance/integration fail-fast on absent creds)
- [x] no prior failures carried forward

### tests executed — with proof

| suite | command | result | proof |
|-------|---------|--------|-------|
| types | `rhx git.repo.test --what types` | ✓ | 🎉 passed (exit 0) |
| format | `rhx git.repo.test --what format` | ✓ | 🎉 passed (exit 0) |
| lint | `rhx git.repo.test --what lint` | ✓ | 🎉 passed (exit 0) |
| unit | `rhx git.repo.test --what unit --mode apply` | ✓ | 🎉 98 passed, 0 failed, 0 skipped (9 suites) |
| integration | `rhx git.repo.test --what integration --mode apply --env test` | ✓ | 🎉 6 passed, 0 failed, 0 skipped (2 suites) |
| acceptance | `rhx git.repo.test --what acceptance --against local --env test --mode apply` | ✓ | 🎉 58 passed, 0 failed, 0 skipped (3 suites) — REAL AWS |

### snapshot change rationalization

| snap | change | intended? | rationale |
|------|--------|-----------|-----------|
| genGetConfig case16 | added | yes | ancestor-optional denied-leaf hard-throw; denials=[api.key], correct narrowed contract |
| genGetConfig case17 | added | yes | nullable-only denied hard-throw; denials=[api.key], correct narrowed contract |
| genGetConfig case23 key | renamed | yes | then-description dash-reworded to satisfy the forbid-gerunds hook; body unchanged |
| genGetConfig case25 | added | yes | denials holds ONLY api.key while errors lists BOTH database.extra_field + api.key — locks non-mis-attribution |

### contract output snapshot exhaustiveness

| contract | positive | negative | edge |
|----------|----------|----------|------|
| sdk getConfig() (unit) | ✓ (case1/13/18/19/20/21) | ✓ (case14/15/16/17/22/23/25) | ✓ (case16/17/22/25) |
| sdk getConfig() (acceptance, real AWS) | ✓ (case7/11/12/13/14 t0/t1) | ✓ (case8/9/10/14 t2/t3) | ✓ (case9/10) |

## residual (not a gate blocker — foreman-provisioned infra)

- **live-AWS IAM AccessDenied classification.** the AccessDeniedException →
  SupplyDeniedError classification is proven at unit level against the EXACT real
  error shape (`name: 'AccessDeniedException'`, supplier unit case5 for both param
  + secret suppliers), and the tolerance flow is proven end-to-end vs REAL AWS for
  the ABSENT (not-found) branch. a proof of a *live* IAM denial vs real AWS needs a
  deliberately-denied resource (an IAM/resource policy that denies the assumed test
  role on a known path) — that is foreman-provisioned infra, not a credential the
  clone can obtain. flagged to the wisher; the residual is narrow (a third-party
  API-contract fact: "real AWS names an authz failure `AccessDeniedException`"),
  already unit-locked.

## open items for the wisher (surfaced, not decided here)

these are design/sign-off items from the i19 l3 reviews that fall OUTSIDE the
approved contract or require human authority — not test-coverage gaps:

- **[wisher sign-off]** two contract narrowings vs the approved 1.vision.yield.md:
  nullable-only no longer null-fills; ancestor-optional no longer drops the node.
  both hard-throw (a correct call — they close real data-loss bugs) but diverge from
  the signed vision. (r10 #1)
- **[wisher sign-off]** prod/cloud now hard-throws on required-denied where before
  schema drift only warned — a real prod blast-radius change. (r10 #1)
- **[design, new scope]** tolerated omissions are cached with no refresh path;
  consider a `getConfig.omissions()` accessor. (r11 #1)
- **[design, new scope]** no log/warn on a tolerated omission; consider a
  `console.debug` that mirrors the schema-drift `console.warn`. (r11 #2)
- **[ergonomic, touches error contract]** the hard-throw hint is generic; for the
  two narrowed cases it could name the precise fix ("mark the LEAF optional, not the
  ancestor" / "use `.nullish()` not `.nullable()`"). (r10 #2)

## peer review round 1 — blockers + nitpicks fixed

the i001 l1 peer review (strict judge: 0 blockers / 0 nitpicks) surfaced 4 blockers + 3
nitpicks. all fixed this round; re-proved green (unit 98/0/0, integration 6/0/0,
acceptance 64/0/0 REAL AWS):

| issue | severity | fix |
|-------|----------|-----|
| r8: supplier unit tests use `jest.fn()` mocks of the AWS SDK client (rule.forbid.unit.remote-boundaries) | 2 blockers | converted BOTH supplier test files to plain-object FAKE clients via DI (no jest.fn); stateful fakes with a call-counter where retry-count is asserted. supplier unit 16/0/0. |
| r5: snapshots embed machine-specific absolute paths in the not-found cases | 1 blocker | dropped `toMatchSnapshot()` for the path-echo errors (genGetConfig case12; blackbox case8 t0/t1, case9) — the message echoes the caller's absolute `statics`/`files`, un-snapshottable portably. kept the `toContain` assertions; removed the obsolete snapshot exports. |
| r2: blackbox (built-artifact) acceptance lacks the new optional/absent/denied behaviors | 1 blocker | added blackbox case10 (optional denied → omitted), case11 (optional absent → omitted), case12 (required denied → hard-throw) against REAL AWS + the public SupplyDeniedError/SupplyAbsentError taxonomy from @src/index; snapshotted (password via `expect.any(String)` matcher). acceptance 58 → 64. |
| r2 + r7: supplier not-found snapshot changed BadRequestError → SupplyAbsentError without documented justification | 3 nitpicks | added a `.note` in both supplier test case4 that states the intentional tolerable-error taxonomy shift (not-found now raises the typed SupplyAbsentError so fill can classify + tolerate). |

r1 (repo-rules) and r3/r4/r6 approved; r9–r11 (l3) await l1 terminal.

### blockers
- none
